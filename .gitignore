# .gitignore
# 只提交核心文件：游戏源码、配置文件、两个工具脚本、文档。
# 其余（谱面音频、调试脚本、打包产物、依赖缓存）一律不传。

# ---------------------------------------------------------------------------
# Python
# ---------------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/

# 虚拟环境
env/
venv/
.venv/
.venv*/

# ---------------------------------------------------------------------------
# 编辑器和操作系统
# ---------------------------------------------------------------------------
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ---------------------------------------------------------------------------
# 游戏资源与运行数据
#   谱面、音频、赛果缓存、Excel 成绩表——都是本机数据，不进仓库
# ---------------------------------------------------------------------------
data/
config/
*.xlsx
*.log
*.pid
*.db

# Pygame 缓存
pygame/

# ---------------------------------------------------------------------------
# 本地依赖缓存（pip 的临时目录在沙箱里常常删不掉，直接忽略）
# ---------------------------------------------------------------------------
wheelhouse/
.piptmp*/
pip-*/
*.whl

# ---------------------------------------------------------------------------
# 打包产物
#   注意：_build/build_ability_exe.py 是打包脚本，要提交；
#   下面几条只挡中间产物、产物和调试用的脚本。
#   注意 .gitignore 不支持行尾注释，注释只能单独占一行。
# ---------------------------------------------------------------------------
dist/
*.zip
*.spec
_build/tmp/
_build/wheels/
_build/standalone/
_build/dist/
_build/pyinstaller*/
# 本机 pip 连不上索引时的临时绕法，不是项目的一部分
_build/install_pyinstaller.py
# 打包产物的验证脚本，属于调试工具
_build/test_ability_exe.py

# ---------------------------------------------------------------------------
# 个人敏感配置
# ---------------------------------------------------------------------------
config/local_*.yaml
secrets.ini
